{
    "version": "0.2.0",
    "configurations": [ 
        {
            "name": "Debug in Chrome",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost:3000/sara-ai",     // create-react-app's default port 3000
            "webRoot": "${workspaceRoot}/src"
        }
    ]
}